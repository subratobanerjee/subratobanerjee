# Date configuration
date_config:
  mode: "today"
  from_date: "2025-01-01"
  to_date: "2025-08-25"
  date_column: "insert_ts"  # Detected date/timestamp candidates [insert_ts, eventtimestamp, date]

datasets:
  - name: wwe2k25{env}.raw.matchroster
    attributes:
      business_unit: ["Sports"]
      data_layer: ["raw"]
      data_owner: ["Data Engineering"]
      pii: false
      priority: ["P1"]
      title_name: ["wwe2k25"]

    checks:
      - name: row_count
      - name: duplicate
        columns:
          - [ai, appgroupid, apppublicid, archetype, buildcl, buildtype, cardidhex, cardinstanceid, careeroverall, cascreationid, city, countrycode, eventcounter, eventdata_sessionid, eventguid, eventid, eventname, eventtimestamp, eventuuid, gamemode, guid, hardware, isgmonline, latitude, longitude, matchguid, matchmodifier, matchtype, modesessionid, mysuperstar_archetype, mysuperstar_background, mysuperstar_ovr, mysuperstar_save_id, mysuperstar_save_slot, name, occurredon, optionsid, orion_char_creationguid, orion_char_id, orion_hof, orion_playerid, orion_quest_chapter, orion_quest_id, orion_quest_name, orion_quest_storyline, orion_rank, orion_screen, orion_sub_screen, orion_unspent_badge_points, orion_unspent_pb_points, orion_vc, orion_xp, payback_1, payback_2, platform, player_loc_x, player_loc_y, player_loc_z, playerpublicid, receivedon, rosterid, rostername, rosteroverall, ruletype, sequencenumber, server_instance_id, server_region, server_relay_region, service, sessionid, sideplate0, sideplate1, sideplate2, sideplate3, slot, submode, submodesessionid, team, useragent, userid, vc, weighttype, wrestlerid, wrestlertype, date] # [insert_ts] removed

threshold_percent: 0
warn_percentage: 0
fail_percentage: 0
