# Date configuration
date_config:
  mode: "today"
  from_date: "2025-01-01"
  to_date: "2025-08-25"
  date_column: "insert_ts"  # Detected date/timestamp candidates [insert_ts, eventtimestamp, date]

datasets:
  - name: wwe2k25{env}.raw.clientdevice
    attributes:
      business_unit: ["Sports"]
      data_layer: ["raw"]
      data_owner: ["Data Engineering"]
      pii: false
      priority: ["P1"]
      title_name: ["wwe2k25"]

    checks:
      - name: row_count
      - name: duplicate
        columns:
          - [appgroupid, apppublicid, buildtype, city, clientlanguage, countrycode, cpu, dx12featurelevel, dx12meshshader, dx12raytracing, dx12resourcebinding, dx12samplerfeedback, dx12shadercache, dx12shadermodel, dx12tiledresources, dx12varrateshading, eventcounter, eventdata_sessionid, eventid, eventname, eventtimestamp, eventuuid, gamemode, gameresheight, gamereswidth, guid, hardware, latitude, longitude, modesessionid, monitorresheight, monitorreswidth, monitors, name, occurredon, optionsid, os, osbuildnumber, osbuildtype, osmem, osversion, "`p.data.diskname`", "`p.data.disksize`", "`p.data.disktype`", "`p.data.freebytes`", "`p.data.totalbytes`", "`p.down.diskname`", "`p.down.disksize`", "`p.down.disktype`", "`p.down.freebytes`", "`p.down.totalbytes`", "`p.temp.diskname`", "`p.temp.disksize`", "`p.temp.disktype`", "`p.temp.freebytes`", "`p.temp.totalbytes`", platform, playerpublicid, receivedon, sequencenumber, service, sessionid, submode, submodesessionid, timezoneoffsetint, useragent, userid, videocard, videomem, date] # [insert_ts] removed

threshold_percent: 0
warn_percentage: 0
fail_percentage: 0
